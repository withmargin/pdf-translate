{
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "build:core": "cargo build --release --manifest-path crates/core/Cargo.toml",
    "build:cli": "pnpm --filter pdf-translate build"
  }
}
